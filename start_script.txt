# basic listing and cd
ls /
cd /docs
ls

# back to root
cd /
ls

# deep navigation
cd /projects/app
ls
cd src
ls
cd ../assets
ls

# find usages
find / *.py
find /projects *main*
cd /
find projects *app*

# cat usages
cat /docs/readme.txt
cat /projects/app/src/main.py

# rmdir tests
# 1) удачное удаление пустой директории (эта директория должна быть в CSV как пустая)
ls /
ls /tmpdir
rmdir /tmpdir
ls /

# 2) попытка удалить не пустую директорию -> ошибка "Directory not empty"
rmdir /projects/app

# 3) попытка удалить файл -> ошибка "<path> is not a directory"
rmdir /docs/readme.txt

# 4) попытка удалить несуществующую -> ошибка "No such directory"
rmdir /no_such

# 5) относительное удаление: переходим в /projects и удаляем пустой дочерний каталог
cd /projects
ls
rmdir emptychild
ls
cd /
ls

# error cases for rmdir (root)
rmdir /
